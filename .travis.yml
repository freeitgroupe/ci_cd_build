sudo: false

before_script :
 - curl -o- -L https://yarnpkg.com/install.sh | bash -s
 - export PATH=$HOME/.yarn/bin:$PATH
 - yarn install

language: node_js
node_js:
  - 9

branches:
  only:
    - master
